const https = require('https');
const fs = require('fs');
const path = require('path');

// 文件路径
const DATA_FILE = path.join(__dirname, '..', 'data', 'reminders.json');
const TOKEN = process.env.TELEGRAM_BOT_TOKEN;
const CHAT_ID = process.env.TELEGRAM_CHAT_ID;

// ==================== 精美时间格式化函数 ====================

/**
 * 获取今天的日期字符串（YYYY-MM-DD格式）
 */
function getTodayDateString() {
    const now = new Date();
    const beijing = new Date(now.getTime() + 8 * 60 * 60 * 1000);
    const year = beijing.getUTCFullYear();
    const month = String(beijing.getUTCMonth() + 1).padStart(2, '0');
    const day = String(beijing.getUTCDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
}

/**
 * 修复日期格式：把带时间的日期转成纯日期
 */
function fixDateFormat(dateStr) {
    if (!dateStr) return dateStr;
    // 如果是 "2026-02-13T12:05:00.000Z" 这种格式，只取前面的日期部分
    if (dateStr.includes('T')) {
        return dateStr.split('T')[0];
    }
    return dateStr;
}

/**
 * 格式化日期为中文显示
 */
function formatDate(date) {
    const now = new Date();
    const beijingNow = new Date(now.getTime() + 8 * 60 * 60 * 1000);
    const today = new Date(beijingNow.getUTCFullYear(), beijingNow.getUTCMonth(), beijingNow.getUTCDate());
    
    const yesterday = new Date(today);
    yesterday.setDate(yesterday.getDate() - 1);
    
    const tomorrow = new Date(today);
    tomorrow.setDate(tomorrow.getDate() + 1);
    
    const targetDate = new Date(date);
    const beijingTarget = new Date(targetDate.getTime() + 8 * 60 * 60 * 1000);
    const targetDay = new Date(beijingTarget.getUTCFullYear(), beijingTarget.getUTCMonth(), beijingTarget.getUTCDate());
    
    if (targetDay.getTime() === today.getTime()) {
        return '今天';
    } else if (targetDay.getTime() === yesterday.getTime()) {
        return '昨天';
    } else if (targetDay.getTime() === tomorrow.getTime()) {
        return '明天';
    } else {
        const month = targetDay.getMonth() + 1;
        const day = targetDay.getDate();
        const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'];
        const weekday = weekdays[targetDay.getDay()];
        return `${month}月${day}日(${weekday})`;
    }
}

/**
 * 计算下一次提醒日期
 */
function calculateNextReminderDate(lastDate, intervalDays) {
    const last = new Date(lastDate);
    const next = new Date(last);
    next.setDate(next.getDate() + intervalDays);
    return next;
}

/**
 * 发送Telegram消息
 */
function sendTelegramMessage(message) {
    return new Promise((resolve) => {
        if (!TOKEN || !CHAT_ID) {
            console.log('❌ 未配置Token或Chat ID');
            resolve(false);
            return;
        }

        const data = JSON.stringify({
            chat_id: CHAT_ID,
            text: message,
            parse_mode: 'HTML',
            disable_web_page_preview: true
        });

        const req = https.request(
            `https://api.telegram.org/bot${TOKEN}/sendMessage`,
            {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Content-Length': Buffer.byteLength(data)
                }
            },
            (res) => {
                let response = '';
                res.on('data', chunk => response += chunk);
                res.on('end', () => {
                    console.log(`✅ Telegram响应: ${res.statusCode}`);
                    resolve(res.statusCode === 200);
                });
            }
        );

        req.on('error', (e) => {
            console.error('❌ 发送失败:', e.message);
            resolve(false);
        });

        req.write(data);
        req.end();
    });
}

// ==================== 主函数 ====================

async function main() {
    console.log('='.repeat(50));
    console.log('🚀 Telegram定时提醒系统 (日期自动修复版)');
    console.log('='.repeat(50));
    
    const today = getTodayDateString();
    const todayDate = new Date(today);
    console.log(`📅 北京时间: ${today} (${formatDate(todayDate)})`);
    
    // 1. 读取提醒数据
    let reminders = [];
    try {
        const rootFile = path.join(__dirname, '..', 'reminders.json');
        if (fs.existsSync(rootFile)) {
            reminders = JSON.parse(fs.readFileSync(rootFile, 'utf8'));
            console.log(`📖 已加载 ${reminders.length} 个提醒`);
            
            // 🔥 关键修复：把所有带时间的日期改成纯日期
            let fixedCount = 0;
            reminders.forEach(r => {
                // 修复 nextReminder
                if (r.nextReminder && r.nextReminder.includes('T')) {
                    r.nextReminder = fixDateFormat(r.nextReminder);
                    fixedCount++;
                }
                // 修复 lastUpdated
                if (r.lastUpdated && r.lastUpdated.includes('T')) {
                    r.lastUpdated = fixDateFormat(r.lastUpdated);
                    fixedCount++;
                }
                if (!r.nextReminder) {
                    const nextDate = calculateNextReminderDate(r.lastUpdated, r.days);
                    r.nextReminder = nextDate.toISOString().split('T')[0];
                    fixedCount++;
                }
                if (r.enabled === undefined) r.enabled = true;
            });
            
            if (fixedCount > 0) {
                console.log(`🔧 已自动修复 ${fixedCount} 个日期格式`);
            }
        } else {
            // 创建默认数据（纯日期格式）
            reminders = [
                { 
                    id: Date.now(), 
                    name: "网宿IP更新", 
                    lastUpdated: today, 
                    nextReminder: today, 
                    days: 3, 
                    enabled: true,
                    createdAt: new Date().toISOString()
                },
                { 
                    id: Date.now() + 1, 
                    name: "周报提交", 
                    lastUpdated: "2026-02-09", 
                    nextReminder: "2026-02-16", 
                    days: 7, 
                    enabled: true,
                    createdAt: new Date().toISOString()
                }
            ];
            console.log('📝 创建默认提醒');
        }
    } catch (e) {
        console.error('❌ 读取失败:', e.message);
        reminders = [];
    }
    
    // 2. 筛选今日到期提醒（现在格式统一了）
    const dueReminders = reminders.filter(r => r.enabled !== false && r.nextReminder === today);
    console.log(`🔍 今日到期提醒: ${dueReminders.length} 个`);
    
    // 3. 发送提醒
    if (dueReminders.length > 0) {
        let message = `📢 <b>每日定时提醒 · ${formatDate(todayDate)}</b>\n\n`;
        message += `━━━━━━━━━━━━━━━━━━━━\n\n`;
        
        dueReminders.forEach((r, index) => {
            const lastDate = new Date(r.lastUpdated);
            const nextDate = calculateNextReminderDate(r.lastUpdated, r.days);
            const nextNextDate = calculateNextReminderDate(r.lastUpdated, r.days * 2);
            
            message += `<b>${index + 1}. ${r.name}</b>\n`;
            message += `   📅 上次更新: ${formatDate(lastDate)}\n`;
            message += `   ⏰ 下次提醒: ${formatDate(nextDate)}\n`;
            message += `   🔄 下下次: ${formatDate(nextNextDate)}\n`;
            message += `   📊 间隔: ${r.days}天\n\n`;
        });
        
        message += `━━━━━━━━━━━━━━━━━━━━\n`;
        message += `💡 请及时处理以上项目\n`;
        message += `⚡ 发送时间: ${new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' })}`;
        
        const success = await sendTelegramMessage(message);
        
        if (success) {
            dueReminders.forEach(r => {
                r.lastUpdated = today;
                const nextDate = calculateNextReminderDate(today, r.days);
                r.nextReminder = nextDate.toISOString().split('T')[0];  // ✅ 保存纯日期
                r.lastNotified = new Date().toISOString();
            });
            console.log('✅ 已更新下次提醒日期');
        }
    } else {
        console.log('ℹ️ 今日无到期提醒');
    }
    
    // 4. 保存数据（强制使用纯日期格式）
    try {
        const dataDir = path.join(__dirname, '..', 'data');
        if (!fs.existsSync(dataDir)) {
            fs.mkdirSync(dataDir, { recursive: true });
        }
        
        // 保存前再确保一次所有日期都是纯格式
        reminders.forEach(r => {
            if (r.nextReminder) r.nextReminder = fixDateFormat(r.nextReminder);
            if (r.lastUpdated) r.lastUpdated = fixDateFormat(r.lastUpdated);
        });
        
        fs.writeFileSync(path.join(dataDir, 'reminders.json'), JSON.stringify(reminders, null, 2), 'utf8');
        fs.writeFileSync(path.join(__dirname, '..', 'reminders.json'), JSON.stringify(reminders, null, 2), 'utf8');
        
        console.log('💾 数据已保存（纯日期格式）');
    } catch (e) {
        console.error('❌ 保存失败:', e.message);
    }
    
    console.log('='.repeat(50));
    console.log('✨ 执行完成');
    console.log('='.repeat(50));
}

main().catch(error => {
    console.error('❌ 程序异常:', error);
    process.exit(1);
});
